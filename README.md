# PizdaBot - Telegram Bot с Пользовательскими Триггерами и Управлением Чата

## Описание

Telegram бот, который отвечает на различные фразы забавными ответами. Поддерживает как глобальные триггеры из кода, так и пользовательские локальные триггеры для каждого чата. **Новинка**: полное управление ботом в чате и настройка вероятности ответов!

## Возможности

### 🔥 Пользовательские триггеры

- Участники чата могут добавлять свои собственные триггеры с ответами
- Пользовательские триггеры имеют приоритет над глобальными
- Лимиты на количество триггеров (3 в день на пользователя, до 50 на чат)
- Валидация длины триггера (2-100 символов) и ответа (1-500 символов)
- Возможность удаления своих триггеров или всех (для админов)

### ⚙️ Управление ботом в чате

- **Полное отключение/включение бота** командами `/off` и `/on`
- **Настройка вероятности ответа** от 0 до 100% командой `/chance`
- **Просмотр настроек чата** командой `/settings`
- **Индивидуальные настройки** для каждого чата сохраняются автоматически

### 📊 Статистика

- Общая статистика использования бота
- Топ самых популярных триггеров
- Статистика за день
- Персональная статистика пользователя

### 👑 Админские функции

- Детальная статистика с информацией по пользователям
- Экспорт статистики в JSON
- Очистка статистики
- Удаление любых пользовательских триггеров
- Сброс настроек чата к значениям по умолчанию

## Команды

### ⚙️ Управление ботом:

- `/on` - включить бота в чате
- `/off` - выключить бота в чате  
- `/chance <0-100>` - установить вероятность ответа в процентах
- `/settings` - показать текущие настройки чата

**Примеры использования управления:**
```
/chance 50    # Бот отвечает в 50% случаев
/chance 0     # Бот не отвечает на триггеры
/chance 100   # Бот отвечает всегда (по умолчанию)
/off          # Полностью отключить бота в чате
/on           # Включить бота обратно
```

### Для всех пользователей:

- `/start` - приветствие бота
- `/help` - справка по командам
- `/stats` - общая статистика
- `/top` - топ-10 триггеров
- `/today` - статистика за сегодня

### Пользовательские триггеры:

- `/add "фраза" "ответ"` - добавить триггер
- `/remove "фраза"` - удалить свой триггер
- `/triggers` - список триггеров чата
- `/my_triggers` - ваша статистика по триггерам

### Только для админов:

- `/admin_stats` - детальная статистика
- `/export_stats` - экспорт статистики в JSON файл
- `/clear_stats` - очистить всю статистику
- `/remove_all_triggers` - удалить все триггеры чата
- `/reset_settings` - сбросить настройки чата

## Установка и настройка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd pizdabot
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте бота:**
   - Создайте бота через @BotFather в Telegram
   - Скопируйте `settings.py.example` в `settings.py`
   - Замените `YOUR_BOT_TOKEN_HERE` на токен вашего бота

4. **Настройте администраторов** (опционально):
   - В файле `app/app.py` в переменной `ADMIN_USERNAMES` укажите username администраторов

5. **Запустите бота:**
   ```bash
   python -m app.app
   ```

## Структура проекта

```
pizdabot/
├── app/
│   ├── __init__.py           # Точка входа модуля
│   ├── app.py               # Основная логика бота
│   ├── statistics.py        # Модуль статистики
│   ├── user_triggers.py     # Менеджер пользовательских триггеров
│   └── chat_settings.py     # Менеджер настроек чата (НОВЫЙ!)
├── triggers.py              # Глобальные триггеры
├── settings.py.example      # Пример настроек
├── requirements.txt         # Зависимости
└── README.md               # Документация
```

## Файлы данных

Бот автоматически создает следующие файлы для хранения данных:

- `bot_stats.json` - статистика использования бота
- `user_triggers.json` - пользовательские триггеры
- `chat_settings.json` - **НОВЫЙ!** настройки каждого чата (включен/выключен бот, вероятность)

## Особенности новой версии

### 🎯 Умное управление

- **Индивидуальные настройки**: каждый чат имеет свои настройки
- **Вероятностные ответы**: настройте частоту срабатывания от 0 до 100%
- **Полное отключение**: команда `/off` полностью отключает бота в конкретном чате
- **Сохранение состояния**: настройки сохраняются между перезапусками

### 🔧 Логика работы

1. **Проверка настроек**: бот сначала проверяет, включен ли он в чате
2. **Вероятность**: если включен, проверяет процент вероятности ответа
3. **Приоритет триггеров**: пользовательские → глобальные
4. **Статистика**: все ответы учитываются в статистике

## Примеры использования

### Базовое использование:
```
Пользователь: /add "привет" "и тебе привет!"
Бот: ✅ Триггер успешно добавлен!

Пользователь: привет
Бот: и тебе привет!
```

### Управление ботом:
```
Администратор: /chance 25
Бот: 🎯 Вероятность ответа установлена: 25%

Пользователь: привет (бот отвечает только в 25% случаев)

Администратор: /off  
Бот: ❌ Бот выключен в этом чате!

Пользователь: привет (бот не отвечает)

Администратор: /on
Бот: ✅ Бот включен в этом чате!
```

### Просмотр настроек:
```
Пользователь: /settings
Бот: ⚙️ Настройки бота в чате:

✅ Статус: включен
🎯 Вероятность ответа: 75%
📅 Последнее изменение: 23.08.2025 19:00
```

## Требования

- Python 3.10+
- aiogram 3.22.0+
- Telegram Bot Token

## Что нового в этой версии

- ✅ **Команды `/off` и `/on`** для полного управления ботом в чате
- ✅ **Команда `/chance`** для настройки вероятности ответа (0-100%)
- ✅ **Команда `/settings`** для просмотра настроек чата
- ✅ **Индивидуальные настройки** для каждого чата с сохранением
- ✅ **Умная логика ответов** с учетом настроек и вероятности
- ✅ **Обновленная справка** с новыми командами